kind: Added
body: 'run precommit-checks: Add command to run local CI-equivalent checks before push, autodetecting `.gitspice/precommit.yaml`, `.pre-commit-config.yaml` (delegates to `pre-commit run --all-files`), or `mise.toml` tasks; with --fix to hand failures to Claude for diagnosis.'
time: 2026-05-04T19:34:41.967003244-07:00
